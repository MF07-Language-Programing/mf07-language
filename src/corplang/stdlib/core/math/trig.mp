"""Trigonometric helpers."""

class Trig {
    static intent sin(x: float): float {
        return mf.math.sin(x)
    }

    static intent cos(x: float): float {
        return mf.math.cos(x)
    }

    static intent tan(x: float): float {
        return mf.math.tan(x)
    }
}
